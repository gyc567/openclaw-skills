import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Understanding AI Skill Pricing",
  description:
    "Learn how to price AI skills effectively. Covers value-based pricing, competitive analysis, freemium strategies, and real revenue data from the AI marketplace.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
